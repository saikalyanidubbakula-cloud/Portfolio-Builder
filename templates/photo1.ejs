<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= portfolio.name %> | Nature Photographer</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;600&family=Cormorant+Garamond:wght@400;700&display=swap">
    <style>
        :root {
            --primary: #4a7043;
            --secondary: #e8ece6;
            --bg: #ffffff;
            --text: #333333;
            --accent: #a8b5a2;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: var(--bg);
            color: var(--text);
            font-family: 'Open Sans', sans-serif;
            line-height: 1.7;
        }
        .page {
            max-width: 1100px;
            margin: 2rem auto;
            padding: 1.5rem;
            background: var(--secondary);
            border-radius: 8px;
        }
        header {
            text-align: center;
            padding: 3rem 1rem;
            background: var(--primary);
            color: #fff;
            border-radius: 8px;
        }
        header h1 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3rem;
            letter-spacing: 1px;
        }
        .download-btn {
            display: inline-block;
            margin: 1rem auto;
            padding: 0.7rem 2rem;
            background: transparent;
            border: 2px solid #fff;
            color: #fff;
            font-family: 'Open Sans', sans-serif;
            font-weight: 600;
            text-decoration: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background 0.3s, color 0.3s;
        }
        .download-btn:hover {
            background: #fff;
            color: var(--primary);
        }
        .content { padding: 2rem 0; }
        .profile {
            text-align: center;
            margin-bottom: 2rem;
        }
        .profile img {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            border: 4px solid var(--primary);
            object-fit: cover;
        }
        .profile h2 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            color: var(--primary);
            margin: 0.5rem 0;
        }
        .profile p { font-size: 1rem; color: var(--text); }
        .section {
            margin: 2rem 0;
            padding: 1.5rem;
            background: #fff;
            border-left: 4px solid var(--primary);
            border-radius: 4px;
        }
        .section h3 {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.8rem;
            color: var(--primary);
            margin-bottom: 0.8rem;
        }
        .section p { font-size: 1rem; }
        .project-files .file-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1rem;
        }
        .project-files img, .project-files video {
            width: 100%;
            height: 180px;
            object-fit: cover;
            border-radius: 4px;
            filter: brightness(0.95);
            transition: filter 0.3s;
        }
        .project-files img:hover, .project-files video:hover {
            filter: brightness(1);
        }
        .project-files audio { width: 100%; margin: 0.5rem 0; }
        .contact a {
            color: var(--primary);
            text-decoration: none;
            transition: color 0.3s;
        }
        .contact a:hover { color: var(--accent); }
        footer {
            text-align: center;
            padding: 1.5rem;
            background: var(--secondary);
            color: var(--text);
            font-size: 0.9rem;
            border-radius: 0 0 8px 8px;
        }
        @media (max-width: 768px) {
            header h1 { font-size: 2.2rem; }
            .profile img { width: 150px; height: 150px; }
            .project-files img, .project-files video { height: 140px; }
        }
        .pdf-hide { display: none !important; }
        @media print {
            body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
            .download-btn { display: none; }
            * { overflow: visible !important; }
            header { background: var(--primary) !important; }
        }
    </style>
</head>
<body>
    <div class="page">
        <header>
            <h1><%= portfolio.name %>: Nature Lens</h1>
            <button class="download-btn" onclick="downloadPDF()">Download as PDF</button>
        </header>
        <div class="content">
            <div class="profile">
                <img src="<%= portfolio.profile_picture || '/Uploads/placeholder.jpg' %>" alt="<%= portfolio.name %>'s Profile">
                <h2><%= portfolio.name %></h2>
                <p><span class="label">Profession:</span> <%= portfolio.profession %></p>
                <p><span class="label">Bio:</span> <%= portfolio.bio || 'Capturing the soul of nature through my lens.' %></p>
            </div>
            <% if (portfolio.camera_skills) { %>
    <div class="section">
        <h3>Camera & Editing Skills</h3>
        <p><%= portfolio.camera_skills %></p>
    </div>
<% } %>

<% if (portfolio.portfolio_link) { %>
    <div class="section">
        <h3>Online Portfolio</h3>
        <p><a href="<%= portfolio.portfolio_link %>" target="_blank"><%= portfolio.portfolio_link %></a></p>
    </div>
<% } %>
            <div class="section projects">
                <h3>Portfolio</h3>
                <% if (portfolio.project_files) { %>
                    <div class="project-files">
                        <h3>Gallery</h3>
                        <div class="file-container">
                            <% portfolio.project_files.split(',').forEach(file => { %>
                                <% if (file.match(/\.(jpg|png|gif)$/i)) { %>
                                    <img src="<%= file %>" alt="Photo" loading="lazy">
                                <% } else if (file.match(/\.(mp4|webm)$/i)) { %>
                                    <video src="<%= file %>" controls loading="lazy"></video>
                                <% } else if (file.match(/\.(mp3|wav)$/i)) { %>
                                    <audio src="<%= file %>" controls></audio>
                                <% } %>
                            <% }); %>
                        </div>
                    </div>
                <% } %>
            </div>
            <div class="section contact">
                <h3>Contact</h3>
                <p><span class="label">Email:</span> <a href="mailto:<%= portfolio.contact_email %>"><%= portfolio.contact_email %></a></p>
                <% if (portfolio.phone) { %>
                    <p><span class="label">Phone:</span> <%= portfolio.phone %></p>
                <% } %>
                <% if (portfolio.linkedin) { %>
                    <p><span class="label">LinkedIn:</span> <a href="<%= portfolio.linkedin %>" target="_blank"><%= portfolio.linkedin %></a></p>
                <% } %>
            </div>
        </div>
    </div>
    <footer><p>© 2025 <%= portfolio.name %> | Nature Captures</p></footer>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
    <script>
        function downloadPDF() {
            const button = document.querySelector('.download-btn');
            button.classList.add('pdf-hide');
            const element = document.querySelector('.page');
            const opt = {
                margin: 0.5,
                filename: '<%= portfolio.name.replace(/\s+/g, "_") %>_portfolio.pdf',
                image: { type: 'jpeg', quality: 0.98 },
                html2canvas: { 
                    scale: window.devicePixelRatio || 2,
                    dpi: 300,
                    useCORS: true,
                    allowTaint: true,
                    logging: true,
                    width: element.scrollWidth,
                    height: element.scrollHeight,
                    style: '*',
                    ignoreElements: (el) => el.classList.contains('pdf-hide')
                },
                jsPDF: { unit: 'pt', format: 'letter', orientation: 'portrait' }
            };
            html2pdf().set(opt).from(element).toPdf().get('pdf')
                .then((pdf) => {
                    button.classList.remove('pdf-hide');
                    pdf.save();
                }).catch((error) => {
                    button.classList.remove('pdf-hide');
                    console.error('PDF generation failed:', error);
                });
        }
    </script>
</body>
</html>