<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= portfolio.name %> | Nature Photographer</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&family=Source+Sans+Pro:wght@300;400&display=swap">
    <style>
        :root {
            --primary: #3c2f5e;
            --secondary: #d4a017;
            --bg: #f7f4f1;
            --text: #1a1a1a;
            --accent: #7b6e9e;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: var(--bg);
            color: var(--text);
            font-family: 'Source Sans Pro', sans-serif;
            line-height: 1.7;
            overflow-x: hidden;
        }
        .page {
            max-width: 1180px;
            margin: 2rem auto;
            padding: 2rem;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
            position: relative;
        }
        header {
            text-align: center;
            padding: 4rem 2rem;
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            color: #fff;
            border-radius: 12px 12px 0 0;
            position: relative;
            overflow: hidden;
        }
        header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://images.unsplash.com/photo-1538370965046-79c0d6907d47?ixlib=rb-4.0.3&auto=format&fit=crop&w=1350&q=80') no-repeat center/cover;
            opacity: 0.25;
            z-index: 0;
        }
        header h1 {
            font-family: 'Libre Baskerville', serif;
            font-size: 3.6rem;
            position: relative;
            z-index: 1;
            letter-spacing: 1px;
            text-shadow: 0 3px 8px rgba(0, 0, 0, 0.3);
        }
        .download-btn {
            display: inline-block;
            margin: 1.2rem auto;
            padding: 0.9rem 2.2rem;
            background: var(--secondary);
            border: none;
            color: var(--text);
            font-family: 'Source Sans Pro', sans-serif;
            font-weight: 400;
            text-decoration: none;
            border-radius: 30px;
            cursor: pointer;
            transition: background 0.3s, transform 0.3s, box-shadow 0.3s;
            position: relative;
            z-index: 1;
        }
        .download-btn:hover {
            background: #e8b923;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }
        .content { padding: 3rem 1rem; }
        .profile {
            text-align: center;
            margin-bottom: 3rem;
            position: relative;
        }
        .profile img {
            width: 210px;
            height: 210px;
            border-radius: 50%;
            border: 6px solid #fff;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            object-fit: cover;
            transition: transform 0.3s ease;
        }
        .profile img:hover { transform: scale(1.06); }
        .profile h2 {
            font-family: 'Libre Baskerville', serif;
            font-size: 2.4rem;
            color: var(--primary);
            margin: 0.8rem 0 0.4rem;
        }
        .profile p {
            font-size: 1.1rem;
            color: var(--text);
            max-width: 650px;
            margin: 0 auto;
        }
        .section {
            margin: 2.5rem 0;
            padding: 2rem;
            background: rgba(247, 244, 241, 0.6);
            border-radius: 10px;
            transition: box-shadow 0.3s ease;
        }
        .section:hover { box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1); }
        .section h3 {
            font-family: 'Libre Baskerville', serif;
            font-size: 2rem;
            color: var(--primary);
            margin-bottom: 1rem;
            position: relative;
        }
        .section h3::after {
            content: '';
            width: 60px;
            height: 2px;
            background: var(--secondary);
            position: absolute;
            bottom: -6px;
            left: 0;
        }
        .section p { font-size: 1.1rem; margin: 0.6rem 0; }
        .project-files .file-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(290px, 1fr));
            gap: 1.8rem;
        }
        .project-files img, .project-files video {
            width: 100%;
            height: 210px;
            object-fit: cover;
            border-radius: 8px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .project-files img:hover, .project-files video:hover {
            transform: scale(1.04);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
        }
        .project-files audio {
            width: 100%;
            margin: 1rem 0;
            filter: brightness(0.9);
        }
        .contact a {
            color: var(--accent);
            text-decoration: none;
            transition: color 0.3s ease;
        }
        .contact a:hover { color: var(--secondary); }
        footer {
            text-align: center;
            padding: 2rem;
            background: var(--primary);
            color: #fff;
            border-radius: 0 0 12px 12px;
            font-size: 1rem;
            font-family: 'Source Sans Pro', sans-serif;
        }
        @media (max-width: 768px) {
            header h1 { font-size: 2.8rem; }
            .profile img { width: 170px; height: 170px; }
            .project-files img, .project-files video { height: 170px; }
            .section { padding: 1.5rem; }
        }
        .pdf-hide { display: none !important; }
        @media print {
            body { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
            .download-btn, header::before { display: none; }
            .section { box-shadow: none !important; }
            * { overflow: visible !important; }
            header { background: var(--primary) !important; }
        }
    </style>
</head>
<body>
    <div class="page">
        <header>
            <h1><%= portfolio.name %>: Nature Lens</h1>
            <button class="download-btn" onclick="downloadPDF()">Download as PDF</button>
        </header>
        <div class="content">
            <div class="profile">
                <img src="<%= portfolio.profile_picture || '/Uploads/placeholder.jpg' %>" alt="<%= portfolio.name %>'s Profile">
                <h2><%= portfolio.name %></h2>
                <p><span class="label">Profession:</span> <%= portfolio.profession %></p>
                <p><span class="label">Bio:</span> <%= portfolio.bio || 'Chasing the fleeting glow of nature’s finest moments.' %></p>
            </div>
            <% if (portfolio.camera_skills) { %>
    <div class="section">
        <h3>Camera & Editing Skills</h3>
        <p><%= portfolio.camera_skills %></p>
    </div>
<% } %>

<% if (portfolio.portfolio_link) { %>
    <div class="section">
        <h3>Online Portfolio</h3>
        <p><a href="<%= portfolio.portfolio_link %>" target="_blank"><%= portfolio.portfolio_link %></a></p>
    </div>
<% } %>
            <div class="section projects">
                <h3>Portfolio</h3>
                <% if (portfolio.project_files) { %>
                    <div class="project-files">
                        <h3>Gallery</h3>
                        <div class="file-container">
                            <% portfolio.project_files.split(',').forEach(file => { %>
                                <% if (file.match(/\.(jpg|png|gif)$/i)) { %>
                                    <img src="<%= file %>" alt="Photo" loading="lazy">
                                <% } else if (file.match(/\.(mp4|webm)$/i)) { %>
                                    <video src="<%= file %>" controls loading="lazy"></video>
                                <% } else if (file.match(/\.(mp3|wav)$/i)) { %>
                                    <audio src="<%= file %>" controls></audio>
                                <% } %>
                            <% }); %>
                        </div>
                    </div>
                <% } %>
            </div>
            <div class="section contact">
                <h3>Contact</h3>
                <p><span class="label">Email:</span> <a href="mailto:<%= portfolio.contact_email %>"><%= portfolio.contact_email %></a></p>
                <% if (portfolio.phone) { %>
                    <p><span class="label">Phone:</span> <%= portfolio.phone %></p>
                <% } %>
                <% if (portfolio.linkedin) { %>
                    <p><span class="label">LinkedIn:</span> <a href="<%= portfolio.linkedin %>" target="_blank"><%= portfolio.linkedin %></a></p>
                <% } %>
            </div>
        </div>
    </div>
    <footer><p>© 2025 <%= portfolio.name %> | Nature Captures</p></footer>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
    <script>
        function downloadPDF() {
            const button = document.querySelector('.download-btn');
            button.classList.add('pdf-hide');
            const element = document.querySelector('.page');
            const opt = {
                margin: 0.5,
                filename: '<%= portfolio.name.replace(/\s+/g, "_") %>_portfolio.pdf',
                image: { type: 'jpeg', quality: 0.98 },
                html2canvas: { 
                    scale: window.devicePixelRatio || 2,
                    dpi: 300,
                    useCORS: true,
                    allowTaint: true,
                    logging: true,
                    width: element.scrollWidth,
                    height: element.scrollHeight,
                    style: '*',
                    ignoreElements: (el) => el.classList.contains('pdf-hide')
                },
                jsPDF: { unit: 'pt', format: 'letter', orientation: 'portrait' }
            };
            html2pdf().set(opt).from(element).toPdf().get('pdf')
                .then((pdf) => {
                    button.classList.remove('pdf-hide');
                    pdf.save();
                }).catch((error) => {
                    button.classList.remove('pdf-hide');
                    console.error('PDF generation failed:', error);
                });
        }
    </script>
</body>
</html>